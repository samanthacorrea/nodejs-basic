// Para executar essa ação rodar o comando "node nome_do_arquivo"
// A mensagem definida abaixo será impressa como saída

console.log('Olá mundo node!! *-*')

// Se a função alert for executada, o código não funcionará
// porque ela está definida apenas no navegador e não roda
// do lado servidor. A seguite mensagem aparecerá caso ela
// seja executada: "ReferenceError: alert is not defined"
// alert('O show tem que continuar')
